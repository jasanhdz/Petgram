import React from 'react'
import ReactDOM from 'react-dom'

// ReactDOM.render(que vamos a renderizar, donde lo vamos a renderizar)
ReactDOM.render(
  <h1>Hello world seguimos avanzando con react</h1>,
  document.getElementById('app')
)
